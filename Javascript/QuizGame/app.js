let count;

const quests = document.querySelector("#question");
const answers = document.querySelectorAll(".btn");

console.log(answers);

answers.forEach(function(answers)
{
    answers.addEventListener("click", function(e)
    {
        const styles = document.get
    });
});